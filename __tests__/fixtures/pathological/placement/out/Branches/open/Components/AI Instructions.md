# AI Instructions

A passthrough component (§7.3): prose copied through verbatim, declaring no sections and
therefore no slots. `IntoPassthrough` in ../Codex/items.cl.yaml targets a slot inside it,
which must report CL0611 naming the reason rather than dropping the item in silence.

Narrate in close third person. Scenes advance on player action.
